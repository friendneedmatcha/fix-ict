info:
  name: update status
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{URL_TEST}}/report/2"
  body:
    type: json
    data: |-
      {
        "status":"SUCCESS",
        "note":"ok in SUCCESS",
        "updatedBy":5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
