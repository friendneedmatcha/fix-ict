info:
  name: create
  type: http
  seq: 3

http:
  method: POST
  url: "{{URL_TEST}}/user/"
  body:
    type: json
    data: |-
      {
        "firstName":"arlif",
        "lastName":"thongrakjan",
        "email":"arlif@gmail.com",
        "password":"arlif555",
        "tel":"1234",
        "profileImage": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
