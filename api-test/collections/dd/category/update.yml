info:
  name: update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{URL_TEST}}/category/1"
  body:
    type: json
    data: |-
      {
        "name":"ITS"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
