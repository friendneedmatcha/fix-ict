info:
  name: create
  type: http
  seq: 3

http:
  method: POST
  url: "{{URL_TEST}}/category"
  body:
    type: json
    data: |-
      {
        "name":"ไฟฟ้า"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
