info:
  name: get by name
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL_TEST}}/category/?name=xx"
  params:
    - name: name
      value: xx
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
