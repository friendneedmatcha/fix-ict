info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL_TEST}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email":"boat@gmail.com",
        "password":"boat555"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
