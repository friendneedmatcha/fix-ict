services:
  nestjs-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nestjs-app

    ports:
      - '3000:3000'

    env_file:
      - .env

    restart: unless-stopped
